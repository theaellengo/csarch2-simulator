{{#each steps}}
<br>
<div class="card bg-light">
  <div class="card-body">
    <h5 class="card-title">Step {{num}}</h5>
    <h6 class="card-subtitle mb-2 text-muted">{{process}}</h6>
    <p class="card-text">{{result}}</p>
  </div>
</div>
{{/each}}